export const isAdult = ( age ) => age >= 18 ? 'Person is an Adult.' : 'Person is not an Adult.'

export const canDrink = ( age ) => age >= 21 ? 'Person can drink.' : 'Person cannot drink.'

export default ( age ) => age >= 65 ? 'Person is a senior.' : 'Person is not a senior.'

